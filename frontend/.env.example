# Leave unset for local dev — requests go through the Vite proxy to localhost:4000.
# In Vercel (or any static host), set this to your deployed backend's origin, e.g.:
# VITE_API_URL="https://api.yourdomain.com"
VITE_API_URL=
